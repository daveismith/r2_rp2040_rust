[package]
name = "cyphal-template"
version = "0.2.0"
edition = "2021"
authors = ["David Smith <1734595+daveismith@users.noreply.github.com>"]
license = "MIT or Apache-2.0"
build = "build.rs"

[features]
default = ["defmt"]
defmt = []

[build-dependencies]
built = { version = "0.8.0", features = ["git2"] }

[dependencies]
embassy-rp = { version = "0.8.0", features = [
    "defmt",
    "unstable-pac",
    "time-driver",
    "critical-section-impl",
    "rp2040",
] }
embassy-embedded-hal = { version = "0.5.0", features = ["defmt"] }
embassy-executor = { version = "0.9.1", features = [
    "arch-cortex-m",
    "executor-thread",
    "executor-interrupt",
    "defmt"
] }
embassy-sync = { version = "0.7.0", features = ["defmt"] }
embassy-time = { version = "0.5.0", features = [
    "defmt",
    "defmt-timestamp-uptime"
] }
embassy-futures = { version = "0.1.1" }
embassy-usb = { version = "0.5.0", features = ["defmt"] }
embassy-usb-logger = { version = "0.5.0" }
embassy-boot-rp = { version = "0.8.0" }

defmt = "1.0.1"
defmt-rtt = "1.0.0"

cortex-m = { version = "0.7.7", features = ["inline-asm"] }
cortex-m-rt = "0.7.5"
panic-probe = { version = "1.0.0", features = ["print-defmt"] }
embedded-alloc = "0.7.0"
static_cell = "2.1.1"
log = { version = "0.4.27", features = ["max_level_info"]}

usb-cli = { path = "../../crates/usb-cli", features = ["rp2040"] }
usb-serial = { path = "../../crates/usb-serial" }

portable-atomic = { version = "1.11.1", features = ["critical-section"] }

embedded-io-async = "0.6.1"
async-trait = "0.1"
embedded-hal = "1.0"
heapless = "0.9.2"
smart-leds = "0.4.0"

canadensis = { version = "0.6.0" }
canadensis_can = { version = "0.6.0" }
canadensis_data_types = { version = "0.6.0" }

#canadensis = { git = "https://github.com/samcrow/canadensis.git", branch = "master" }
#canadensis_can = { git = "https://github.com/samcrow/canadensis.git", branch = "master" }
#canadensis_data_types = { git = "https://github.com/samcrow/canadensis.git", branch = "master" }

embedded-can = "0.4.1"
mcp25xx = { version = "0.4.0", features = ["mcp25625"] }

[package.metadata.embassy]
build = [
    { target = "thumbv6m-none-eabi" }
]